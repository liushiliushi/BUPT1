#ifndef ABC
#define ABC
#include "head.h"
extern int money,ridernumber,time,temp,accept,over,finish;//非静态全局变量和起初始值的定义 
extern struct order * firstorder;//头订单指针 
extern struct rider allriders[50];//所有骑手信息 
#endif
